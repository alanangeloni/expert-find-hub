import { Link } from 'react-router-dom';

const Footer = () => {
  return (
    <footer className="bg-transparent">
      <div className="container mx-auto px-4 py-8">
        <div className="hidden">
          {/* This keeps the layout but hides the content */}
        </div>
      </div>
    </footer>
  );
};

export default Footer;
